# شرح التعامل مع الملفات والوقت والتاريخ والمنطقة الزمنية في PHP

هذه الوثيقة تقدم لك شرحًا **واضحًا ومنظمًا** لجميع النقاط التي طلبتها،
مع أمثلة جاهزة ومبسطة.

------------------------------------------------------------------------

## 📌 أولاً: التعامل مع الملفات في PHP

### 1️⃣ قراءة ملف

تستخدم هذه الدالة لجلب جميع محتويات الملف دفعة واحدة.

``` php
$content = file_get_contents("file.txt");
echo $content;
```

✓ مناسبة للملفات الصغيرة والمتوسطة.

------------------------------------------------------------------------

### 2️⃣ كتابة ملف (إنشاء أو استبدال)

``` php
file_put_contents("file.txt", "Hello World!");
```

✓ إذا كان الملف موجودًا سيتم استبدال محتواه.\
✓ إذا لم يكن موجودًا سيتم إنشاؤه.

------------------------------------------------------------------------

### 3️⃣ إضافة محتوى دون حذف الموجود

``` php
file_put_contents("file.txt", "New Line\n", FILE_APPEND);
```

✓ يُستخدم عند الحاجة لإضافة معلومات جديدة دون التأثير على القديمة.

------------------------------------------------------------------------

### 4️⃣ الفتح التقليدي للملفات (قراءة سطر بسطر)

``` php
$file = fopen("data.txt", "r");
while (!feof($file)) {
    echo fgets($file);
}
fclose($file);
```

✓ مناسب للملفات الكبيرة لتجنب تحميل الملف بالكامل في الذاكرة.

------------------------------------------------------------------------

### 📝 أهم أوضاع فتح الملفات

  الوضع    الوظيفة
  -------- ----------------------------
  `"r"`    قراءة فقط
  `"w"`    كتابة ويمسح المحتوى القديم
  `"a"`    كتابة مع الإضافة
  `"r+"`   قراءة + كتابة
  `"a+"`   قراءة + كتابة مع إضافة

------------------------------------------------------------------------

### 5️⃣ حذف ملف

``` php
unlink("file.txt");
```

✓ يحذف الملف نهائيًا.

------------------------------------------------------------------------

### 6️⃣ التحقق من وجود ملف

``` php
if (file_exists("file.txt")) {
    echo "الملف موجود";
}
```

✓ مفيد قبل محاولة فتح أو قراءة ملف لتجنب الأخطاء.

------------------------------------------------------------------------

## 📌 ثانياً: دوال الوقت والتاريخ في PHP

### 1️⃣ معرفة الوقت الحالي

``` php
echo date("Y-m-d H:i:s");
```

✓ يعرض: سنة-شهر-يوم + ساعة:دقيقة:ثانية

------------------------------------------------------------------------

### 2️⃣ تنسيقات مختلفة للتاريخ

``` php
echo date("l");        // اسم اليوم
echo date("d-m-Y");    // اليوم-الشهر-السنة
echo date("h:i A");    // الوقت مع AM/PM
```

✓ يمكن دمج أي تنسيق تريده.

------------------------------------------------------------------------

### 3️⃣ Unix Timestamp

``` php
echo time();
```

✓ عدد الثواني منذ 1970-01-01.

------------------------------------------------------------------------

### 4️⃣ تحويل Timestamp إلى تاريخ

``` php
echo date("Y-m-d", 1700000000);
```

------------------------------------------------------------------------

### 5️⃣ إضافة أو طرح وقت

``` php
echo date("Y-m-d H:i:s", strtotime("+3 days"));
echo date("Y-m-d H:i:s", strtotime("-2 hours"));
```

✓ للتحكم بالوقت بشكل احترافي.

------------------------------------------------------------------------

## 📌 ثالثًا: التعامل مع المنطقة الزمنية (Time Zone)

### لماذا مهمّة؟

لأن PHP قد تضبط المنطقة الزمنية بشكل افتراضي مختلف عن بلدك، مما يؤدي
لظهور وقت خاطئ.

------------------------------------------------------------------------

### 1️⃣ ضبط المنطقة الزمنية (مثال اليمن)

``` php
date_default_timezone_set("Asia/Aden");
```

------------------------------------------------------------------------

### 2️⃣ عرض المنطقة الزمنية الحالية

``` php
echo date_default_timezone_get();
```

------------------------------------------------------------------------

### 3️⃣ أشهر المناطق الزمنية

  الدولة     المنطقة الزمنية
  ---------- -----------------
  اليمن      Asia/Aden
  السعودية   Asia/Riyadh
  مصر        Africa/Cairo
  الإمارات   Asia/Dubai

------------------------------------------------------------------------

### 4️⃣ استخدام كائن DateTime (أفضل وأدق)

``` php
$date = new DateTime("now", new DateTimeZone("Asia/Aden"));
echo $date->format("Y-m-d H:i:s");
```

------------------------------------------------------------------------

## 📌 رابعًا: حساب الفرق بين تاريخين

### مثال: حساب عدد الأيام بين تاريخين

``` php
$date1 = new DateTime("2025-01-01");
$date2 = new DateTime("2025-02-10");

$diff = $date1->diff($date2);
echo $diff->days;
```

✓ يظهر لك عدد الأيام بين التاريخين.

------------------------------------------------------------------------

## 🎯 ملخص سريع

-   **file_get_contents** لقراءة ملف كامل بسهولة\
-   **file_put_contents** للكتابة أو الإضافة\
-   **date()** للحصول على الوقت بتنسيقات مختلفة\
-   **strtotime()** للتعامل مع الوقت بشكل ديناميكي\
-   **DateTime** للأعمال الاحترافية\
-   **date_default_timezone_set** لضبط الوقت حسب بلدك

------------------------------------------------------------------------

هل تحتاج إضافة أمثلة متقدمة أو قسم التمارين؟
