# شرح شامل: الاتصال بقاعدة البيانات MySQL باستخدام PHP (خطوة بخطوة)

هذا الدليل يشرح **بشكل مفصل جدًا** كيفية الاتصال بقاعدة بيانات **MySQL** باستخدام لغة **PHP**، مع شرح المفاهيم، الخطوات، الأمثلة، والأخطاء الشائعة.

---

## 1) ما هو MySQL؟

**MySQL** هو نظام إدارة قواعد بيانات علائقي (RDBMS) يُستخدم لتخزين البيانات مثل:
- المستخدمين
- المنتجات
- الرسائل
- الطلبات

ويُستخدم غالبًا مع PHP في تطبيقات الويب.

---

## 2) ما هو الاتصال بقاعدة البيانات؟

الاتصال بقاعدة البيانات يعني:
> إنشاء قناة تواصل بين كود PHP وقاعدة بيانات MySQL

حتى يتمكن البرنامج من:
- إضافة بيانات (INSERT)
- قراءة بيانات (SELECT)
- تعديل بيانات (UPDATE)
- حذف بيانات (DELETE)

---

## 3) المتطلبات الأساسية (الخطوة الأولى)

قبل كتابة أي كود يجب توفر الآتي:

1. تثبيت **PHP**
2. تثبيت **MySQL Server**
3. تشغيل خادم محلي مثل:
   - XAMPP
   - WAMP
   - LAMP
4. إنشاء قاعدة بيانات من phpMyAdmin أو MySQL

مثال إنشاء قاعدة بيانات:
```sql
CREATE DATABASE test_db;
```

---

## 4) بيانات الاتصال (مهمة جدًا)

أي اتصال بـ MySQL يحتاج 4 معلومات أساسية:

| العنصر | الشرح | مثال |
|------|------|------|
| Host | مكان السيرفر | localhost |
| Username | اسم المستخدم | root |
| Password | كلمة المرور | (فارغة غالبًا) |
| Database | اسم القاعدة | test_db |

---

## 5) طرق الاتصال بقاعدة البيانات في PHP

في PHP توجد **3 طرق مشهورة**:

1. MySQL (قديمة ❌ – لا تُستخدم)
2. MySQLi (جيدة ✅)
3. PDO (الأفضل ⭐)

---

## 6) الاتصال باستخدام MySQLi (الطريقة الكائنية)

### الخطوات:
1. تعريف بيانات الاتصال
2. إنشاء كائن الاتصال
3. فحص الاتصال

```php
<?php
$host = "localhost";
$user = "root";
$pass = "";
$db   = "test_db";

$conn = new mysqli($host, $user, $pass, $db);

if ($conn->connect_error) {
    die("فشل الاتصال: " . $conn->connect_error);
}

echo "تم الاتصال بقاعدة البيانات بنجاح";
?>
```

---

## 7) الاتصال باستخدام MySQLi (الطريقة الإجرائية)

```php
<?php
$conn = mysqli_connect("localhost", "root", "", "test_db");

if (!$conn) {
    die("فشل الاتصال: " . mysqli_connect_error());
}

echo "تم الاتصال بنجاح";
?>
```

---

## 8) الاتصال باستخدام PDO (الموصى به)

### لماذا PDO؟
- أكثر أمانًا
- يدعم أكثر من نوع قاعدة بيانات
- يحمي من SQL Injection

```php
<?php
try {
    $conn = new PDO("mysql:host=localhost;dbname=test_db;charset=utf8", "root", "");
    $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
    echo "تم الاتصال بنجاح";
} catch (PDOException $e) {
    echo "فشل الاتصال: " . $e->getMessage();
}
?>
```

---

## 9) تنفيذ استعلامات SQL بعد الاتصال

### 9.1 قراءة البيانات (SELECT)

```php
$sql = "SELECT * FROM users";
$result = $conn->query($sql);

while ($row = $result->fetch_assoc()) {
    echo $row['username'] . "<br>";
}
```

---

## 10) الحماية باستخدام Prepared Statements

### مثال تسجيل دخول آمن:

```php
$stmt = $conn->prepare("SELECT * FROM users WHERE email = ? AND password = ?");
$stmt->bind_param("ss", $email, $password);

$email = "test@test.com";
$password = "123456";

$stmt->execute();
$result = $stmt->get_result();

if ($result->num_rows > 0) {
    echo "تم تسجيل الدخول";
} else {
    echo "بيانات خاطئة";
}
```

---

## 11) فصل الاتصال في ملف مستقل (أفضل ممارسة)

### ملف: config.php

```php
<?php
$conn = new mysqli("localhost", "root", "", "test_db");
if ($conn->connect_error) {
    die("فشل الاتصال");
}
?>
```

### الاستخدام:
```php
require "config.php";
```

---

## 12) أخطاء شائعة جدًا

| الخطأ | السبب | الحل |
|------|------|------|
| Access denied | كلمة مرور خاطئة | تأكد من البيانات |
| Unknown database | القاعدة غير موجودة | أنشئ القاعدة |
| mysqli not found | الامتداد غير مفعل | فعل MySQLi |

---

## 13) نصائح مهمة للمبتدئين

- لا تستخدم MySQL القديمة ❌
- استخدم PDO أو MySQLi فقط ✅
- لا تثق بمدخلات المستخدم
- افصل الاتصال عن منطق التطبيق

---

## 14) خلاصة

✔ الاتصال بقاعدة البيانات هو أساس أي تطبيق PHP
✔ PDO هو الخيار الأفضل
✔ الأمان مهم جدًا

---

📘 **هذا الشرح مكتوب بصيغة Markdown (.md)** ومناسب للدراسة أو التسليم الأكاديمي.

