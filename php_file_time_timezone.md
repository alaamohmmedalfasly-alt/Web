# التعامل مع الملفات والوقت والتاريخ والمنطقة الزمنية في PHP

## 📌 أولاً: التعامل مع الملفات في PHP

### 1. قراءة ملف

``` php
$content = file_get_contents("file.txt");
echo $content;
```

### 2. كتابة ملف (إنشاء أو استبدال)

``` php
file_put_contents("file.txt", "Hello World!");
```

### 3. إضافة محتوى دون مسح القديم

``` php
file_put_contents("file.txt", "New Line\n", FILE_APPEND);
```

### 4. فتح ملف والتعامل معه بالطريقة التقليدية

``` php
$file = fopen("data.txt", "r");
while (!feof($file)) {
    echo fgets($file);
}
fclose($file);
```

### أوضاع فتح الملفات

  الوضع   الوظيفة
  ------- ---------------------
  r       قراءة فقط
  w       كتابة مع حذف القديم
  a       كتابة مع الإضافة
  r+      قراءة وكتابة
  a+      قراءة وإضافة

### 5. حذف ملف

``` php
unlink("file.txt");
```

### 6. التحقق من وجود ملف

``` php
if (file_exists("file.txt")) {
    echo "الملف موجود";
}
```

------------------------------------------------------------------------

## 📌 ثانيًا: دوال الوقت والتاريخ في PHP

### 1. الوقت الحالي

``` php
echo date("Y-m-d H:i:s");
```

### 2. تنسيقات الوقت

``` php
echo date("l");        // اسم اليوم
echo date("d-m-Y");    // اليوم-شهر-سنة
echo date("h:i A");    // الوقت مع AM/PM
```

### 3. Unix Timestamp

``` php
echo time();
```

### 4. تحويل Timestamp إلى تاريخ

``` php
echo date("Y-m-d", 1700000000);
```

### 5. إضافة أو طرح وقت

``` php
echo date("Y-m-d H:i:s", strtotime("+3 days"));
echo date("Y-m-d H:i:s", strtotime("-2 hours"));
```

------------------------------------------------------------------------

## 📌 ثالثًا: التعامل مع المنطقة الزمنية (TimeZone)

### 1. ضبط المنطقة الزمنية

``` php
date_default_timezone_set("Asia/Aden");
```

### 2. عرض المنطقة الزمنية الحالية

``` php
echo date_default_timezone_get();
```

### 3. أمثلة لمناطق زمنية

  الدولة     المنطقة الزمنية
  ---------- -----------------
  اليمن      Asia/Aden
  السعودية   Asia/Riyadh
  مصر        Africa/Cairo
  الإمارات   Asia/Dubai

### 4. استخدام DateTime

``` php
$date = new DateTime("now", new DateTimeZone("Asia/Aden"));
echo $date->format("Y-m-d H:i:s");
```

------------------------------------------------------------------------

## 📌 رابعًا: حساب الفرق بين تاريخين

``` php
$date1 = new DateTime("2025-01-01");
$date2 = new DateTime("2025-02-10");

$diff = $date1->diff($date2);
echo $diff->days;
```
